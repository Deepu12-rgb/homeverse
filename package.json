{
  "name": "homeverse",
  "version": "1.0.0",
  "description": "Homeverse real estate application",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "start-backend": "cd backend && npm start",
    "start-frontend": "cd frontend && http-server -p 3000",
    "install-deps": "npm install && cd backend && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "real-estate",
    "homeverse",
    "property"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "http-server": "^14.1.1"
  }
} 